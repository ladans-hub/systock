# Build artifacts

`Systock.ipa` version `1.0.5+6` was generated on 2026-09-17 with the configured Apple distribution certificate and App Store provisioning profile (`P4NJH9UZG6`).

The macOS `.pkg` was not generated because the installed Xcode toolchain is missing the Metal SDK used by `mobile_scanner`. The Windows `.exe` requires a Windows build runner; this macOS environment cannot produce it natively.
